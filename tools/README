- This setup is for Ubuntu 14.04.
- Each script assumes that if the file of that package exists, the package is
  already installed. If for some reason the install failed, delete the folder
(and tar.gz) to try again.

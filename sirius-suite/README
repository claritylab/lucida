Johann Hauswald
Vinicius Petrucci
jahausw@umich.edu
2014

Sirius Suite
Each kernel contains (makefile included for each):
+input (when applicable)
+baseline
+pthread
+GPU
+PHI
+bits: this folder contains trial or old versions

Each benchmark is run the following way:
./<executable name> ../input/<input>

Regex takes 2 inputs:
./regex ../input/list ../input/questions

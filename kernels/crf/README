./autogen.sh
./configure
make && make install
sudo ./go.sh

You can use go.sh to compile new + run or look at the script to see what it does,
and just run the example.
go.sh is used when making modifications to wrap make + install into 1 script
